import React from 'react';
import { HomePageContainer } from './styled';

const Home: React.FC = () => {
  return (
    <HomePageContainer>
      <h1>Bem-vindo à página inicial!</h1>
    </HomePageContainer>
  );
};

export default Home;
